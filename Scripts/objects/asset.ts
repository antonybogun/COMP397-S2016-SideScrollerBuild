module objects {
    /**
     * Holds main asset manifest for the game
     *
     * @export
     * @interface Asset
     */
    export interface Asset {
        id: string;
        src: string;
    }
}